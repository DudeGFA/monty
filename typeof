#include<stdlib.h>
#include<stdio.h>
#include<stdbool.h>
#include "monty.h"

bool digit_check(char key[])
{
    for(int i = 0; i < strlen(key); i++)
    {
        if(isdigit(key[i])==0)
        {
            return false;
        }
    }
    return true;
}